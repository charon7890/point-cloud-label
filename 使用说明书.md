# 点云叶片标注 — 使用说明书

本工具在浏览器里查看本机点云，并把同一片叶子在不同日期文件中对应成同一个编号。标注结果写在点云 txt 末尾的 `leaf_id` 列，以及一份 `leaf_labels.json`。

本地服务地址：`http://127.0.0.1:8765`

---

## 1. 启动

1. 双击项目目录中的 `start.bat`。
2. 若尚未创建环境，脚本会自动建立 `PC_label`（Python 3.11）并安装依赖。
3. 启动后会自动打开浏览器。若没有打开，请手动访问 `http://127.0.0.1:8765`。
4. 若页面按钮、文案看起来不是最新版，按 **Ctrl+F5** 强制刷新。

关闭：在「完成」对话框保存成功后点「退出」，或直接关掉运行 `start.bat` 的窗口。

---

## 2. 导入点云

支持扩展名：`.txt`、`.xyz`、`.ply`、`.pcd`。程序会递归扫描子文件夹，并按文件名或上级文件夹里的日期（如 `20250915`）排序。点云按全量加载，不做抽样。

本数据常用格式（ASCII）：

```text
x y z inst_class sem_class R G B nx ny nz inst_prob sem_prob
```

导入方式任选其一：

| 方式 | 说明 |
| --- | --- |
| **选择文件夹** | 弹出系统文件夹窗口，记下真实磁盘路径，便于之后直接保存。 |
| **打开本机路径** | 输入路径后点「加载」，例如 `E:\pepper_results\K02`。 |
| **拖入文件夹** | 把文件夹拖到首页卡片上。程序会尽量对上本机原目录。 |

导入后界面分为三栏：

- **左侧**：点云文件列表（日期、文件名、大小、是否已对应叶片）
- **中间**：三维窗口
- **右侧**：叶片对应列表

点「重新导入」会回到首页，可换另一个文件夹。浏览器会暂存当前文件夹的标注，再次打开同一批文件时会尽量恢复。

---

## 3. 推荐标注流程

目标：同一片叶子在所有日期里都叫同一个号（1号、2号、3号…）。未对应的实例导出时 `leaf_id` 为 `0`。

1. 打开第一帧点云。着色保持「按实例」。
2. **单击**尚未对应的实例。未选中任何叶片时，会自动新建「1号叶片」并完成对应；该实例变为半透明。
3. 继续单击其他未对应实例，会依次得到 2号、3号…
4. 按 **→** 切到下一帧。
5. 在右侧点选要对应的叶片（或按 **↑ / ↓**），再单击本帧中同一片叶子的实例。
6. 也可先回到已经对应过的那一帧，**单击已对应的实例**以选中该叶片，再切到其他帧去点同一片叶子。
7. 所有需要对应的叶子都标完后，点右下角 **完成**。

要点：

- 未选中叶片时，单击**已对应**的实例：只选中该叶片，不会新建。
- **已选中**某叶片时，单击任意实例：把该实例划给当前叶片（若它原先属于别的叶片，会改过来）。
- 当前文件里已经对应过的实例会半透明，避免重复点选。
- 每个点云文件里，一片叶子只对应一个实例。

---

## 4. 三维操作

| 操作 | 作用 |
| --- | --- |
| 按住左键拖动 | 旋转 |
| 滚轮 | 缩放 |
| 按住右键拖动 | 平移 |
| 中键拖动 | 缩放（轨道控制） |
| 单击左键（几乎不移动） | 选中实例并标注 |
| 双击右键（不拖动） | 进入「拖动视角」模式：此后左键改为平移 |
| **Esc** | 退出拖动视角模式 |
| 点大小滑条 | 调整点的显示大小 |
| 着色下拉框 | 按实例 / 原始颜色 / 按高度 / 语义类别 |
| **复位视角** | 回到适合当前点云的默认视角 |

WASD 是屏幕方向平移，与相机朝向无关：

| 键 | 方向 |
| --- | --- |
| **W** | 上 |
| **S** | 下 |
| **A** | 左 |
| **D** | 右 |

可按住连续移动，可组合（例如 W+A 斜向上）。

---

## 5. 快捷键一览

快捷键在标注界面生效。首页、完成对话框打开时，或正在输入路径、下拉框聚焦时无效。按住 Ctrl / Alt / Win 时也不触发。

### 浏览与选择

| 快捷键 | 作用 |
| --- | --- |
| **←** | 上一个点云文件 |
| **→** | 下一个点云文件 |
| **↑** | 上一片叶片（选中） |
| **↓** | 下一片叶片（选中） |
| **M** | 新增叶片并选中（号码取当前最小空缺号） |

### 视角

| 快捷键 | 作用 |
| --- | --- |
| **W / A / S / D** | 屏幕平移：上 / 左 / 下 / 右 |
| **Esc** | 退出「拖动视角」模式，并松开 WASD |

### 删除

| 快捷键 | 作用 |
| --- | --- |
| **Delete** | 只取消**当前文件**里，当前选中叶片的对应 |
| **Shift + Delete** | 删除整片叶片（所有文件中的对应都去掉，小号会回收） |

删除前必须先在右侧选中目标叶片（单击列表，或 ↑↓，或单击已对应的实例）。

---

## 6. 叶片列表按钮

右侧选中某叶片后，会出现：

| 按钮 | 作用 |
| --- | --- |
| **新增叶片 (M)** | 与按 M 相同 |
| **取消对应** | 与 Delete 相同：只取消当前文件 |
| **删除叶片** | 与 Shift+Delete 相同：整片删除并回收编号 |

列表中会显示该叶片是否已在当前文件对应，以及已覆盖多少个点云文件。

---

## 7. 保存与另存为

点 **完成** 后出现对话框（不会立刻弹出新文件夹窗口）。

| 按钮 | 作用 |
| --- | --- |
| **保存** | 把带 `leaf_id` 的 txt 和 `leaf_labels.json` **写回打开这些文件的原文件夹**。同名 txt 会被覆盖。 |
| **另存为** | 弹出系统文件夹窗口（可新建文件夹），写到所选位置，不改原文件。 |
| **取消** | 关闭对话框，继续标注。 |

保存或另存为成功后：

| 按钮 | 作用 |
| --- | --- |
| **退出** | 关闭本地服务，可关掉浏览器页面 |
| **导入新文件夹** | 回到首页，开始下一批 |

### 写出内容

- 每个点云 txt：**保留原来所有列**，在末尾增加 `leaf_id`。
  - 已对应：该叶片的统一编号（跨文件相同）
  - 未对应：`0`
- 若文件已经有 `leaf_id` 列，会更新最后一列，不会再追加一列。
- 同目录写入 `leaf_labels.json`（叶片列表与每文件对应关系）。

请用「选择文件夹」或「打开本机路径」导入，这样保存能直接写回原目录。

---

## 8. 界面其他说明

- 左侧文件可直接单击切换，不必只用方向键。
- 「已就绪 / 加载中」表示后台预读进度；未点开的文件也会陆续载入。
- 标注会暂存在浏览器本地。换电脑或清站点数据后，以磁盘上保存的 txt / json 为准。
- 三维窗口需要联网加载 Three.js（CDN）。断网时页面可能无法显示点云。

---

## 9. 常见问题

**保存提示找不到原文件夹**  
用「选择文件夹」或「打开本机路径」重新导入后再保存。拖入后一般也能自动对上，若不行请改用前两种方式。

**点了实例却变成新叶片，而不是接到已有叶片上**  
先在右侧选中目标叶片（或 ↑↓），再单击。未选中叶片时，单击未对应实例会新建叶片。

**Delete 没有反应**  
需要先选中叶片。Delete 只取消当前这一帧的对应；要整片删掉请用 Shift+Delete 或「删除叶片」。

**WASD 转着转着方向乱了**  
当前实现是屏幕平移，不会绕物体打转。若进入了拖动视角模式，按 Esc 退出后再试。

**页面功能不像说明书**  
Ctrl+F5 刷新。确认 `start.bat` 启动的是本项目里的服务。
