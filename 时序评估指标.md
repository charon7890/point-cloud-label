# 多时相叶片身份评估：指标、公式与论文表述

本文与 `eval_leaf_tracks.py`（CLI：`eval_temporal.py`）一致。相对初版多数票邻日准确率，本套指标按多目标跟踪（MOT / MOTS）惯例重写，使欠分割、过分割、漏跟、跨天串号分别落在不同数字上，便于投稿。

```powershell
E:\点云标注\PC_label\python.exe E:\点云标注\eval_temporal.py --gt <真值文件夹> --pred <预测文件夹>
```

---

## 0. 为什么改指标

初版用「真值叶片当天点数最多的预测号」当身份，再比相邻两天号是否相同。这有三个不适合写进论文的问题：

1. **欠分割被算成跟对。** 11 号并进 10 号后，11 号天天多数票都是 10，邻日 10→10，准确率 100%。跟踪文献里这应是漏检 / 合并，不是成功关联。
2. **过分割几乎看不见。** 只要主块仍过半，碎片不改变多数票。
3. **缺少可引用的主指标。** 审稿人预期看到 HOTA、IDF1，而不是自创的多数票。

现协议在每一天用 **点集 IoU + 匈牙利一对一匹配**（阈值 0.5）决定真值叶片对上了哪条预测轨迹。匹配失败（含合并中输掉的那一片）记为未匹配，**不再当作身份一致**。HOTA 把「当天找没找到」和「跨天是不是同一条轨迹」拆成 DetA 与 AssA。

在注入错误的 K10-WR 上：多数票 AssocAcc 为 94.42%；匹配式 AssocAcc 为 92.09%，差额几乎全部来自被合并的 11 号（14 天全程未匹配）。这正是改指标的目的。

---

## 1. 问题与数据

给定日期序列 \(t=1,\ldots,T\) 上的植株点云。真值与预测为同一批点、同一行顺序；只评估真值 `leaf_id` \(>0\) 的点（未标注点不进对错）。每片真值叶片 \(k\) 是一条轨迹，寿命为它被标注的那些天。

第 \(t\) 天：真值点集 \(G_{k,t}\)，预测身份 \(c\) 的点集 \(P_{c,t}\)（仅在已标注点上计）。点集 IoU：

\[
\mathrm{IoU}_{t}(k,c)=\frac{\lvert G_{k,t}\cap P_{c,t}\rvert}{\lvert G_{k,t}\cup P_{c,t}\rvert}.
\]

**逐帧匹配** \(\mathcal{M}_t^{(\alpha)}\)：在 \(\mathrm{IoU}\ge\alpha\) 的候选上做最大权匈牙利，一对一。默认 \(\alpha=0.5\)（与 MOTS 一致）。未匹配的真值叶片记 \(m_t(k)=\varnothing\)；匹配到预测号 \(c\) 则 \(m_t(k)=c\)。

编号置换（全程 1 号被叫做 5 号）在匹配后不影响关联：每天都是 \(m_t(k)\) 相同即可。

---

## 2. 论文主指标

投稿时建议主表只放这些（多株时报 mean±std）：

| 符号 | 名称 | 作用 |
|------|------|------|
| HOTA | Higher Order Tracking Accuracy | 检测×关联的综合分，主数字 |
| DetA / AssA | 检测精度 / 关联精度 | 拆开「切开」与「跟住」 |
| IDF1 | ID F1 | 全局轨迹级身份 F1 |
| AssocAcc | 邻日关联准确率（匹配式） | 最好解释的一句人话 |
| IDSW | 身份切换次数 | 中途改号 |
| Split / Merge | 过分割率 / 欠分割率 | 单日拆开 / 粘连 |
| PQ | 全景质量 | 单日实例分割 |
| mIoU\(_\mathrm{frame}\) / mIoU\(_\mathrm{global}\) | 逐扫描 mIoU / 全局身份 mIoU | 时序代价 |

### 2.1 HOTA（Luiten et al., IJCV 2021）

在阈值 \(\alpha\in\{0.05,0.10,\ldots,0.95\}\) 上分别匹配。记 TP 为匹配成功的真值检测，FN、FP 为未匹配真值 / 预测检测。

\[
\mathrm{DetA}_\alpha=\frac{\lvert\mathrm{TP}\rvert}{\lvert\mathrm{TP}\rvert+\lvert\mathrm{FN}\rvert+\lvert\mathrm{FP}\rvert}.
\]

对每个 TP \(c\)（某天、某真值 \(k\)、某预测 \(p\)），令 \(\mathrm{TPA}(c)\) 为「同一 \(k\) 且同一 \(p\)」的 TP 个数（含自身）；\(\mathrm{FNA}(c)\) 为真值 \(k\) 的检测里不在 TPA 中的个数；\(\mathrm{FPA}(c)\) 为预测 \(p\) 的检测里不在 TPA 中的个数。

\[
A(c)=\frac{\mathrm{TPA}(c)}{\mathrm{TPA}(c)+\mathrm{FNA}(c)+\mathrm{FPA}(c)},\qquad
\mathrm{AssA}_\alpha=\frac{1}{\lvert\mathrm{TP}\rvert}\sum_{c\in\mathrm{TP}}A(c).
\]

\[
\mathrm{HOTA}_\alpha=\sqrt{\mathrm{DetA}_\alpha\cdot\mathrm{AssA}_\alpha},\qquad
\mathrm{HOTA}=\frac{1}{19}\sum_{\alpha}\mathrm{HOTA}_\alpha.
\]

LocA 为 TP 的平均 IoU。HOTA@0.5 即 \(\alpha=0.5\) 的单点。

**读法：** DetA 高、AssA 低 = 当天分得开，跨天跟不稳。K10-WR：DetA 0.960、AssA 0.903、HOTA 0.931。

### 2.2 IDF1（Ristani et al., ECCV 2016）

在 \(\alpha=0.5\) 的逐帧匹配上，用匈牙利把真值轨迹与预测轨迹按共同匹配次数做一次全局对齐，得到 \(\sigma\)。与 \(\sigma\) 一致的检测计为 IDTP。

\[
\mathrm{IDF1}=\frac{2\,\mathrm{IDTP}}{2\,\mathrm{IDTP}+\mathrm{IDFP}+\mathrm{IDFN}}.
\]

IDP、IDR 为对应的精确率 / 召回率。IDF1 惩罚「整段跟错对象」，对长轨迹中途改号敏感。

### 2.3 邻日关联准确率 AssocAcc（匹配式，可解释主指标）

日历相邻、且两天真值都出现的叶片 \(\mathcal{S}_{t,t+1}\)：

\[
\mathrm{OK}(k,t,t+1)=\mathbf{1}\!\bigl[m_t(k)\neq\varnothing,\; m_{t+1}(k)\neq\varnothing,\; m_t(k)=m_{t+1}(k)\bigr].
\]

\[
\mathrm{AssocAcc}=\frac{\sum_{t}\sum_{k\in\mathcal{S}_{t,t+1}}\mathrm{OK}(k,t,t+1)}{\sum_{t}\lvert\mathcal{S}_{t,t+1}\rvert}.
\]

未匹配（漏跟、合并中输掉的叶）算失败，不算成功。不要求 \(m_t(k)=k\)。

- **加权** \(\mathrm{AssocAcc}_w\)：权重为两天点数均值。
- **宏平均** \(\mathrm{AssocAcc}_\mathrm{macro}\)：先对每叶再平均，小叶与大叶同等。
- **PairAcc**：同一叶任意两天（不限相邻）身份是否相同。中途改一次号会让改号前×改号后的所有日期对失败，因此通常低于 AssocAcc。

### 2.4 ID Switch

CLEAR MOT：真值 \(k\) 在某天匹配到 \(p\)，若距上一次成功匹配的预测号不同，计 1 次 IDSW。漏跟本身不计 switch；恢复后若换了号才计。

\[
\mathrm{IDSWRate}=\frac{\mathrm{IDSW}}{\#\{\text{该叶两次连续成功匹配}\}}.
\]

单日错配通常进、出各 1 次，该叶 IDSW = 2。

### 2.5 过分割 / 欠分割

某天真值叶 \(k\) 上，若至少两个预测号各占该叶点数 \(\ge 10\%\)，计一次 split。某预测号上若至少两片真值叶各占该预测点数 \(\ge 10\%\)，计一次 merge。

\[
\mathrm{SplitRate}=\frac{\#\text{split 的真值检测}}{\#\text{真值检测}},\qquad
\mathrm{MergeRate}=\frac{\#\text{merge 的预测检测}}{\#\text{预测检测}}.
\]

K10-WR：split 0.16%（2 号在一天被拆出 72 号），merge 2.28%（11 号并入 10 号）。

### 2.6 PQ 与 mIoU

PQ（Kirillov et al., CVPR 2019），\(\alpha=0.5\)：

\[
\mathrm{SQ}=\frac{1}{\lvert\mathrm{TP}\rvert}\sum \mathrm{IoU},\quad
\mathrm{RQ}=\frac{\lvert\mathrm{TP}\rvert}{\lvert\mathrm{TP}\rvert+\tfrac12\lvert\mathrm{FP}\rvert+\tfrac12\lvert\mathrm{FN}\rvert},\quad
\mathrm{PQ}=\mathrm{SQ}\cdot\mathrm{RQ}.
\]

- **mIoU\(_\mathrm{frame}\)**：每天单独最大权匹配后，对真值叶平均 IoU（未匹配为 0），再对天平均。只反映单日分割。
- **mIoU\(_\mathrm{global}\)**：全序列点数混淆矩阵上做一次匈牙利，把预测号映到真值号后再算各类 IoU 的平均。跨天串号会拉低。
- **时序代价** \(\Delta=\mathrm{OA}_\mathrm{frame}-\mathrm{OA}_\mathrm{global}\)。K10-WR 为 6.74%。

点级 ARI 在有效点上用原始 \((g_i,p_i)\) 列联表计算（不做匈牙利），衡量划分是否同类。

### 2.7 关联随间隔衰减（建议作图）

对所有扫描对 \((t,t+\delta)\)，用与 AssocAcc 相同的 OK 规则，按帧间隔 \(\delta\) 或日历天数分箱平均。论文图：AssocAcc–\(\delta\)。间隔越大越难跟，曲线应下降。K10-WR：相邻扫描约 92%，15 天以上约 72%（3/4 号中途对调主要伤长间隔）。

### 2.8 MOTA（附表即可）

\[
\mathrm{MOTA}=1-\frac{\mathrm{FN}+\mathrm{FP}+\mathrm{IDSW}}{\#\text{真值检测}}.
\]

可取负，且把检测与切换混在一个减法里，不宜作主指标；HOTA 已更合理。

---

## 3. ARI

列联表 \(n_{uv}\)，\(N=\sum n_{uv}\)，\(a_u=\sum_v n_{uv}\)，\(b_v=\sum_u n_{uv}\)：

\[
\mathrm{ARI}=\frac{\sum_{u,v}\binom{n_{uv}}{2}-\frac{(\sum_u\binom{a_u}{2})(\sum_v\binom{b_v}{2})}{\binom{N}{2}}}{\frac12(\sum_u\binom{a_u}{2}+\sum_v\binom{b_v}{2})-\frac{(\sum_u\binom{a_u}{2})(\sum_v\binom{b_v}{2})}{\binom{N}{2}}}.
\]

叶–日 ARI 的样本是 \((t,k)\)：真值标签为 \(k\)，预测标签为 \(m_t(k)\)，未匹配用 \(-k\)（各叶漏跟不合成一个大的 0 类）。

---

## 4. 错误类型与指标（自检）

| 现象 | HOTA | DetA | AssA | AssocAcc | IDSW | Split | Merge |
|------|------|------|------|----------|------|-------|-------|
| 全程换号 | 不变 | 不变 | 不变 | 不变 | 0 | — | — |
| 中途两叶对调 | 降 | 几乎不变 | 降 | 跨越日降 | +1/叶 | — | — |
| 单日错配 | 略降 | 几乎不变 | 略降 | 进、出两天降 | +2/叶 | — | — |
| 后几天漏标 | 降 | 降 | 降 | 降 | 0 | — | — |
| 欠分割（并叶） | 降 | 降 | 视匹配而定 | **输掉的叶全失败** | 0 | — | 升 |
| 过分割（碎块） | 略降 | 略降 | 主块仍稳则可不变 | 可变可不变 | 可变 | 升 | — |

---

## 5. K10-WR 控制实验（指标 sanity check）

真值 `K10`，预测为注入错误的 `K10-WR`（编号对调、中途串号、单日错配、漏标、欠分割、过分割）。不是真实算法，只用来确认指标方向正确。

| 指标 | 数值 |
|------|------|
| HOTA (DetA, AssA) | 0.931 (0.960, 0.903) |
| HOTA@0.5 | 0.936 |
| IDF1 | 0.939 |
| AssocAcc@0.5 | 0.921（512/556） |
| 多数票 AssocAcc（旧，不主报） | 0.944 |
| IDSW | 26 |
| Split / Merge | 0.002 / 0.023 |
| PQ | 0.979 |
| mIoU frame / global | 0.965 / 0.940 |
| 时序代价 | 0.067 |

不稳定叶片与注入一致：3/4 中途对调；6 组单日错配各 2 次 switch；15 号漏跟；**11 号全程未匹配（欠分割，旧指标会漏报）**。1↔5 编号对调不出现在不稳定列表中。

---

## 6. 多株实验怎么报

每株一行上述主指标，最后一行 mean±std。比较两种算法时，在植株维上做 Wilcoxon 符号秩检验（\(n\) 较小时不要只报均值）。AssocAcc–间隔曲线可对全部扫描对汇总，或先按株平均再平均。

---

## 7. 输出文件

默认 `<预测文件夹>/temporal_eval/`：

- `paper_table.csv`：主表一行，可直接贴
- `summary.json`：全部标量
- `adjacent_pairs.csv`：每一对相邻扫描
- `per_leaf.csv`：每片真值叶的匹配序列（`∅` 为未匹配）
- `assoc_by_gap.csv`：按帧间隔 / 日历天数
- `hota_by_alpha.csv`：HOTA–\(\alpha\) 曲线

---

## 8. Methods（English, paste-ready）

**Evaluation protocol.** We evaluate multi-temporal leaf identity on sequences of plant point clouds. Predicted and ground-truth clouds contain the same points in the same order; scores are restricted to points with a ground-truth leaf identity \(>0\). Each annotated leaf is treated as a track spanning the scans in which it is labelled. In every scan we match ground-truth leaves to predicted identities by the Hungarian algorithm that maximises point-set IoU, retaining pairs with \(\mathrm{IoU}\ge\alpha\). Matching is one-to-one: a predicted identity cannot be assigned to two ground-truth leaves in the same scan. Predicted IDs need not equal ground-truth IDs; only consistency of the matched ID across time is required.

**Higher Order Tracking Accuracy.** We report HOTA (Luiten *et al.*, 2021). At each IoU threshold \(\alpha\in\{0.05,\ldots,0.95\}\), detection accuracy \(\mathrm{DetA}_\alpha\) is the Jaccard index of matched versus unmatched leaf detections, and association accuracy \(\mathrm{AssA}_\alpha\) is the mean, over true-positive detections, of the Jaccard index between detections that share both the same ground-truth ID and the same predicted ID. \(\mathrm{HOTA}_\alpha=\sqrt{\mathrm{DetA}_\alpha\cdot\mathrm{AssA}_\alpha}\), and HOTA is the mean over \(\alpha\). We also list \(\mathrm{HOTA}_{50}\), localisation accuracy (mean IoU of matches), and the usual detection/association precision–recall terms. This decomposition separates per-scan recognition from temporal identity.

**Identity-level scores.** IDF1 (Ristani *et al.*, 2016) is computed at \(\alpha=0.5\) after a second, sequence-level Hungarian assignment of predicted tracks to ground-truth tracks that maximises the number of matched detections. Adjacent association accuracy (AssocAcc) is the fraction of ground-truth leaves present in two consecutive scans for which both detections are matched and receive the same predicted ID; unmatched leaves, including the loser of a merge, count as errors. We additionally report a size-weighted AssocAcc, a macro-average over leaves, and PairAcc over all pairs of days in which a leaf appears. Identity switches (IDSW) follow CLEAR MOT: a switch is counted when a ground-truth leaf is matched to a different predicted ID than at its previous successful match.

**Segmentation diagnostics.** Per-scan panoptic quality (PQ; Kirillov *et al.*, 2019) and mIoU after frame-wise matching isolate spatial instance quality. Global mIoU uses a single ID mapping estimated from the concatenated confusion matrix; the gap between frame-wise and global overall accuracy is a temporal cost that grows when identities drift. Split (resp. merge) rate is the fraction of ground-truth (resp. predicted) detections whose point mass is shared, at \(\ge 10\%\) each, by two or more identities. Association accuracy as a function of inter-scan interval (frame step and calendar days) is reported as a curve.

**Implementation notes.** Unlabelled points do not enter any denominator. Leaves are evaluated only on days they are annotated (no penalty before emergence). Unless stated, AssocAcc and IDF1 use \(\alpha=0.5\).

---

## 9. Results 句式（English, 可改数字）

**Controlled error test (optional sanity-check paragraph).** To verify that the metrics respond to the intended failure modes, we copied a fully annotated sequence and injected controlled identity errors: a global ID permutation, a mid-sequence swap of two leaves, single-scan misassignments, late-sequence deletions, an under-segmentation merge, and a one-scan over-segmentation. Frame-wise detection remained high (\(\mathrm{DetA}=0.960\), \(\mathrm{PQ}=0.979\)), while association dropped (\(\mathrm{AssA}=0.903\), \(\mathrm{AssocAcc}=0.921\), 26 ID switches). The merged leaf was unmatched at every scan under IoU matching (and would have been counted as correct under a majority-vote rule). The global ID permutation did not reduce AssocAcc. Association accuracy declined with inter-scan interval, from about 0.92 on adjacent scans to about 0.72 at gaps of 15 days or more.

**Real method (template).** On \(N\) plants, the proposed tracker attains HOTA \(x.xx{\pm}x.xx\) (DetA \(x.xx\), AssA \(x.xx\)) and IDF1 \(x.xx{\pm}x.xx\). Adjacent AssocAcc is \(x.xx{\pm}x.xx\). Per-scan mIoU is \(x.xx\), falling to \(x.xx\) when a single identity mapping is required over the whole sequence (temporal cost \(x.xx\)). Failure cases are dominated by [identity switches / merges / misses] …

---

## 10. 建议主表表头

`Plant | HOTA | DetA | AssA | IDF1 | AssocAcc | IDSW | Split | Merge | PQ | mIoU-fr | mIoU-gl`

图：AssocAcc（或 AssA）对扫描间隔；可选 HOTA–\(\alpha\)。

不要把多数票 AssocAcc 和 MOTA 放进主表；需要时放补充材料，并写明 majority-vote AssocAcc 会把欠分割算成成功关联。
